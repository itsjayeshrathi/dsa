#include <bits/stdc++.h>
using namespace std;

// int maximumEnergy(vector<int> &energy, int k)
// {
//     int maxEnergy = INT_MIN;
//     for (int i = 0; i < energy.size(); i++)
//     {
//         int temp = energy[i];
//         for (int j = i + k; j < energy.size(); j += k)
//         {
//             temp += energy[j];
//         }

//         maxEnergy = max(temp, maxEnergy);
//     }
//     return maxEnergy;
//
// }

int main()
{
    vector<int> v = {-2, -3, -1};
    int k = 2;
    //int maxEnergy = maximumEnergy(v, k);
    //cout << maxEnergy << endl;
    return 0;
}